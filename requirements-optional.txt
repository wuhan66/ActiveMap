-r requirements-ml.txt

# Optional backbones/augmentation frameworks. The reference implementation does
# not require these packages, but reuse adapters may use them on the server.
segmentation-models-pytorch>=0.3.4,<1
albumentations>=1.4,<3
timm>=1.0,<2
lightning>=2.4,<3
torchmetrics>=1.4,<2
